import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chrisis.ionic',
  appName: 'chrisisfirstapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
